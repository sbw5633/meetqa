import 'package:flutter/material.dart';

class BodyLeft extends StatelessWidget {
  const BodyLeft({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black.withOpacity(0.1),
    );
  }
}
